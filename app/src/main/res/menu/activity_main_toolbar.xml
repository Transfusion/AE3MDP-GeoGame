<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_refresh_tasks"
        android:title="Generate a new set of tasks"
        android:icon="@drawable/ic_refresh_24dp"
        app:showAsAction="always"/>

    <item android:id="@+id/action_task_history"
        android:title="Task History"
        android:icon="@drawable/ic_history_24dp"
        app:showAsAction="always"/>

    <item android:id="@+id/action_game_settings"
        android:title="Game Settings"
        android:icon="@drawable/ic_settings_24dp"
        app:showAsAction="always"/>

    <item android:id="@+id/action_quit_game"
        android:title="Stop background service and quit game"
        android:icon="@drawable/ic_exit_to_app_24dp"
        app:showAsAction="always"/>

</menu>